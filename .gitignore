coverage/
dist/
node_modules/
out/
test-results/
data.json
settings.json

# Copied from the repo's canonical assets by `npm run site:assets`.
# site/assets/og.png and icon.png are committed — they are generated by
# `npm run site:images`, not copied.
site/assets/demo.gif
site/.vercel/
